@using Microsoft.AspNetCore.Identity
@using Recycling.Models
@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers


@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">你好， @User.Identity.Name!</a>
    </li>
    <li class="nav-item">
        <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="/" method="post">
            <button type="submit" class="nav-link btn btn-link text-dark">退出</button>
        </form>
    </li>
}
else
{
    <a class="btn btn-sm btn-outline-secondary mx-1" href="Identity/Account/Register">注册</a>
    <a class="btn btn-sm btn-outline-secondary mx-1" href="Identity/Account/Login">登录</a>
}